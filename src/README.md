# Shared Source Code

Reusable project code shared by more than one analysis phase belongs here.

Phase-specific orchestration should remain under `workflows/`.

Historical F0–F2 scripts remain frozen under `foundation/` and should not be silently refactored in place.