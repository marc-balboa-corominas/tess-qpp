# Phase 3A design freeze v1

**STATUS:** `SCIENTIFIC DESIGN FROZEN — COHORT MATERIALIZATION NOT STARTED`

This directory is the prospective scientific design produced by F3A.1 after the closed
Bibliographic Audit II gate `F3A_DESIGN_RECONSIDERATION_REQUIRED`.

The design freezes:

- one primary external catalogue source and deterministic cohort-construction rule;
- observational reference-label semantics without ground-truth claims;
- the deliberate F2-derived 13 × 6 primary robustness matrix (78 cells);
- the W00/P00 baseline-reproduction gate;
- explicit admissibility, classification, period, and denominator contracts;
- numerical-stability testing at W00/P00 over seeds 0–9;
- a disposition for all 11 BAII comparator candidates;
- positioning and interpretation boundaries.

The status is `PHASE3A_SCIENTIFIC_DESIGN_FROZEN_WITH_DOCUMENTED_LIMITATION` because the exact
machine-readable BAIIW0001 event table/schema and native TESS-product mapping have not yet been
physically materialized. That verification belongs to F3A.2 and is mechanical/provenance work:
failure blocks materialization and requires a versioned design amendment; it does not authorize
silent source substitution.

No cohort has been materialized, no TESS light curves have been opened/downloaded for F3A,
AFINO has not been executed for F3A, and no F3A scientific result has been computed.
