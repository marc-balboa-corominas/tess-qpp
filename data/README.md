# Data

Large observational data and regenerated numerical artifacts are not versioned directly in this repository.

This directory is reserved for:

- provenance documentation;
- acquisition instructions;
- immutable identifiers;
- small reference tables when scientifically justified.

Raw TESS products, large arrays, checkpoints, and runtime caches must remain outside Git.